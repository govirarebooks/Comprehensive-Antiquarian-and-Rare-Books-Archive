# Govi Rare Books RAG

This directory contains the Retrieval-Augmented Generation (RAG) pipeline
for the Govi Rare Books Archive dataset.

The pipeline will be designed to support incremental updates as the archive
grows.
